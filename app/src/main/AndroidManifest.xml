<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andrewvora.apps.mynpu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.andrewvora.apps.mynpu.OurApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="" />

        <activity
            android:name="com.andrewvora.apps.mynpu.activities.HomeActivity"
            android:windowSoftInputMode="adjustResize"
            android:alwaysRetainTaskState="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.andrewvora.apps.mynpu.activities.MapActivity"
            android:windowSoftInputMode="adjustResize"
            android:alwaysRetainTaskState="true">
        </activity>

        <activity
            android:name="com.andrewvora.apps.mynpu.activities.AboutActivity"
            android:alwaysRetainTaskState="true">
        </activity>

        <activity
            android:name="com.andrewvora.apps.mynpu.activities.ScheduleActivity"
            android:alwaysRetainTaskState="true">
        </activity>

        <activity
            android:name="com.andrewvora.apps.mynpu.activities.SettingsActivity"
            android:alwaysRetainTaskState="true">
        </activity>

    </application>

</manifest>
